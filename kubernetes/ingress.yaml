apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: omnistream-ingress
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: omnistream-service
            port:
              number: 80
