apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: omnistream-servicemonitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: omnistream
  endpoints:
  - port: "http"
    path: "/metrics"
    interval: 15s
